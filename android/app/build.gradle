plugins { id 'com.android.application' }

android {
    namespace 'mr.bankily.app'
    compileSdk 35

    defaultConfig {
        applicationId 'mr.bankily.app'
        minSdk 24
        targetSdk 35
        versionCode 2
        versionName '1.0.0'
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
